sudo: required
services:
  - docker
install: false
jobs:
  include:
    - stage: continuous_integration
      if: NOT tag IS present
      script: make install && make lint && make test && make build
    - stage: deployment
      if: tag IS present
      script: make install && make lint && make test && make build && make publish
      