sudo: required
services:
  - docker
install: make prepare
stages:
  - name: ci
    if: (NOT tag = /.*/) AND (NOT branch = migration)
    script:
      - make lint
      - make test
      - make build
  - name: cd
    if: branch = migration
    script:
      - make lint
      - make test
      - make build
      - make publish