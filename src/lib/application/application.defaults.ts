import type { ApplicationOptions } from "./application.schema";

export const DEFAULT_AUTHOR: ApplicationOptions['author'] = '';

export const DEFAULT_DESCRIPTION: ApplicationOptions['description'] = '';

export const DEFAULT_LANGUAGE: ApplicationOptions['language'] = 'ts';

export const DEFAULT_VERSION: ApplicationOptions['version'] = '0.0.1';

export const DEFAULT_HTTP_PLATFORM_APP: ApplicationOptions['platform'] = 'express';
